# Message for the menu entry
sylius:
    menu:
        admin:
            main:
                catalog:
                    customer_options: Customer Options
                    customer_option_groups: Customer Option Groups


# Brille24
brille24:
    ui:
        customer_options: Customer Options
        customer_option_groups: Customer Option Groups
        required: Required
        product_customization: Product customization
        pricing:
            fixed_amount: Fixed amount
            percent: Percent
    form:
        customer_options:
            subheader: Manage your Customer Options
            unique: The code has to be unique
            not_null: The value cannot be blank
            required: This option is required
            type:
                text: Text
                select: Select
                multi_select: Multi Select
                file: File
                date: Date
                datetime: Datetime
                number: Number
                boolean: Boolean
        customer_option_groups:
            subheader: Manage your Customer Option Groups
            select: Select
            duplicate_option: A group can not have an option twice
        config:
            min:
                length: Minimum length
                file_size: Minimum size
                date: Minimum date
                number: Minimum number
            max:
                length: Maximum length
                file_size: Maximum size
                date: Maximum date
                number: Maximum number
            invalid_min_max: The configurations minimum value is greater then the maximum
            default_value: Default value
        validators:
            condition:
                greater: ">"
                greater_equal: ">="
                equal: "=="
                lesser_equal: "<="
                lesser: "<"
                in_set: Is in set
                not_in_set: Is not in set
            fields:
                customer_option: Customer Option
                comparator: Comparator
                value:
                    default: Value
                    text: Text length
                    set: Set
            error_message: Error message

    grid:
        options_remaining: "{0} |]0,Inf]and %count% more"
        options_empty: Nothing to display
